# database configuration
DB_DATABASE=
DB_USER=
DB_PASSWORD=
DB_HOST=

# Shared database configuration (for OAuth tokens shared with gm-frontend)
SHARED_DB_NAME=
SHARED_DB_USER=
SHARED_DB_PASSWORD=
SHARED_DB_HOST=

# Encryption key for OAuth tokens (must be same as gm-frontend)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# request documentation configuration
EXPIRY_DAYS=

MY_BASE_URL=

# newsfeed configuration
NEWSFEED_MAX_RESULTS=50

#CRM
CRM_AUTHORIZATION_TOKEN=


#EMAIL
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_TO=


#SCHEDULERS
# Documentation Expiration Scheduler Configuration
# Cron schedule for expiring documentation (default: '0 0 * * *' = midnight daily)
# Examples:
# '0 0 * * *'     - Every day at midnight
# '0 2 * * *'     - Every day at 2:00 AM
# '0 0 * * 0'     - Every Sunday at midnight
# '*/30 * * * *'  - Every 30 seconds (for testing)
EXPIRE_DOCS_SCHEDULE=

# Timezone for the scheduler (default: 'Europe/Lisbon')
# Examples:
# 'Europe/Lisbon'
# 'America/New_York'
# 'Europe/London'
# 'UTC'
EXPIRE_DOCS_TIMEZONE=

#GOOGLE DRIVE (OAuth - tokens created via gm-frontend)
# Client ID and Secret (must be same as gm-frontend)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Redirect URI (not used by requestdocumentation, only by gm-frontend for OAuth flow)
GOOGLE_REDIRECT_URI=
# Default folder ID for uploads
GOOGLE_DEFAULT_FOLDER=

# Legacy service account fields (no longer used)
# GOOGLE_PROJECT_ID=
# GOOGLE_PRIVATE_KEY_ID=
# GOOGLE_CLIENT_EMAIL=
# GOOGLE_PRIVATE_KEY=